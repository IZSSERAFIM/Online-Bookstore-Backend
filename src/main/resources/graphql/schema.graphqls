type Query {
    getBookByTitle(title: String!): Book
}

type Book {
    id: ID!
    title: String!
    author: String!
    description: String
    price: Int
    cover: String
    sales: Int
    stock: Int
}